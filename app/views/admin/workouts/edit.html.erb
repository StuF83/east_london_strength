<%= render 'form', 
    workout: @workout,
    page_title: "Edit Workout - #{@workout.date.strftime('%b %d, %Y')}",
    header_links: [
      { text: 'View', path: admin_workout_path(@workout), class: 'btn btn--secondary' },
      { text: 'Back to List', path: admin_workouts_path, class: 'btn btn--secondary' }
    ],
    submit_text: 'Update Workout',
    cancel_path: admin_workout_path(@workout) %>